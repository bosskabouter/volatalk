import React from 'react';
import PropTypes from 'prop-types';
import { IContact } from 'types';

function VideoBox(props: { message: IContact }) {
  return (
    <>
      <video></video>
    </>
  );
}

export default VideoBox;
