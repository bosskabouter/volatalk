import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#F6F6F6',
      main: '#e5a50a',
      dark: '#707070',
    },
    secondary: {
      main: '#612385',
    },
  },
});
export default theme;
