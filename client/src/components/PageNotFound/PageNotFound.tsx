/** @jsxImportSource @emotion/react */

import { Typography } from '@mui/material';

const PageNotFound = () => {
  return <Typography>Page Not Found: The page you are looking for does not exist.</Typography>;
};

export default PageNotFound;
