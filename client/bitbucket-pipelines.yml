image: node:12

pipelines:
  default:
    - step:
        name: "Install dependencies"
        caches:
          - node
        script:
          - npm install
    - step:
        name: "Build"
        caches:
          - node
        script:
          - npm run build
